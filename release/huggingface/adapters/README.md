# Adapter weights staging area

This local release candidate contains exactly the 12 canonical safetensors
packages listed in `../adapter_manifest_verified.csv`. Conversion, exact
round-trip equality, base-model loading, 256×256 CPU forward tests, and
metadata/security scans passed on 2026-07-20. Licensing approval remains
required before publication. Never include `CellViT-SAM-H-x40.pth` here.
